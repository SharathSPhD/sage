# Findings — the anomaly log

Anomalies are the product. Every unexpected meter reading lands here with the config that produced it and whether it was chased. Negative results are written plainly, not dressed up.

Entry format:

```
## F-NNNN — <one-line description>
- Date:
- Instrument / experiment:
- Config: <path to resolved config or gate artifact>
- Expected / observed:
- Chase status: chasing | parked (why) | resolved (what it was)
- Resolution / follow-up:
```

## F-0001 — ℛ exceeds 1 on matching pennies (ℛ is not a fraction)

- Date: 2026-08-08
- Instrument / experiment: `reciprocity_defect` via `experiments/reciprocity_calibration.py`
- Config: `config/experiments/reciprocity_calibration.yaml` (seed 20260808, λ = 1.2)
- Expected / observed: informal expectation was ℛ ∈ [0,1]; matching pennies reads ℛ = 1.2. The definition ‖χ−χᵀ‖/‖χ+χᵀ‖ is a norm *ratio*, unbounded above — values > 1 mean the antisymmetric (circulating) part of the equilibrium response dominates the symmetric part.
- Chase status: resolved (definitional, not a bug). RPS-3 reads 0.69, RPS-5 reads 0.43, MP reads 1.20 — the 2×2 tangent space (1-D per player) makes MP the most purely rotational response geometry available.
- Resolution / follow-up: docs corrected to state ℛ ∈ [0, ∞). Worth watching in the α-sweep whether ℛ > 1 is exclusive to (near-)pure harmonic games with minimal action spaces — if a *mixed* game ever reads > 1 at moderate α, that is structure worth understanding.

## F-0002 — ℛ magnitude scales with λ (only the zero/nonzero reading is λ-free)

- Date: 2026-08-08
- Instrument / experiment: `reciprocity_defect`, red-team probe during unit review (matching pennies, λ ∈ {0.5, 1, 2, 4})
- Expected / observed: ledger/docs briefly claimed "ℛ is λ-free"; observed ℛ(λ) ≈ ∝ λ on matching pennies (0.5, 1.0, 2.0, ... at λ = 0.5, 1.0, 2.0). Consistent with χ_T = (I − S_T B_T)⁻¹S_T with S ∝ λ: the antisymmetric part enters at O(λ²) against a symmetric O(λ) base, so the ratio grows ≈ linearly at small λ.
- Chase status: resolved (theory understood; docs/ledger corrected). The λ-free property is Result 2's *symmetry* statement: ℛ = 0 ⟺ potential, at every λ.
- Resolution / follow-up: empirical use of ℛ levels (Stage 3 pass-through work) must hold λ fixed or co-report it; only the zero test is λ-robust. Candidate for a normalised variant ℛ/λ_normalised if cross-λ comparability becomes needed.

## F-0003 — EPR, ℛ and α co-move almost perfectly on random 3×3 families

- Date: 2026-08-08
- Instrument / experiment: `thermo_read` + `reciprocity_defect` via `experiments/dynamics_calibration.py`
- Config: `config/experiments/dynamics_calibration.yaml` (seed 20260808, λ = 1.2, 100×10 games)
- Expected / observed: C1 predicted co-movement but not its tightness; observed ρ(EPR, ℛ) = 0.993 — the dissipation of the *joint-profile Markov chain* and the asymmetry of the *equilibrium response matrix* are near-interchangeable orderings on these families, despite being computed from entirely different objects (generator vs resolvent).
- Chase status: superseded by F-0004 — the marginal agreement is α-driven confounding, discovered by red-team stratification.
- Resolution / follow-up: see F-0004.

## F-0004 — The meters DECOUPLE at high α: within-level ρ(EPR, ℛ) reverses sign

- Date: 2026-08-08
- Instrument / experiment: stratified re-analysis of `chain_comovement` (red-team objection during dynamics.exact review; independently verified same day, then made a permanent artifact metric)
- Config: `config/experiments/dynamics_calibration.yaml` (seed 20260808, λ = 1.2, 100 games × 10 α levels)
- Expected / observed: C1 expected tight co-movement of dissipation and reciprocity defect. Observed: the marginal ρ(EPR, ℛ) = 0.993 is driven by α. **Conditional on α**, coupling is strong (+0.80..+0.88) for α ≤ 0.65, degrades through 0.75 (+0.61) and 0.85 (+0.32), and **reverses to −0.355 at α = 0.95**: among near-pure-harmonic games, higher reciprocity defect associates with LOWER dissipation.
- Chase status: **resolved by F-0007** (mechanism experiment, same games, paired design). Half the hypothesis held: ℛ's high-α variation IS denominator-driven (ρ(ℛ, 1/‖χ+χᵀ‖) = 0.993 at α = 0.95). But the numerator fix fails — see F-0007.
- Resolution / follow-up: (1) test the hypothesis by tracking numerator and denominator of ℛ separately along α; (2) check whether an EPR-normalised or numerator-only variant restores coupling at high α; (3) verify on N=3 and larger m; (4) this belongs in p3_noneq as a result, with C1's falsifier status stated plainly.

## F-0005 — Blotto is mixed, not pure harmonic: α = 0.69 on the symmetric budget-3 game

- Date: 2026-08-08
- Instrument / experiment: `alpha` via `experiments/blotto_calibration.py` (seed 20260808)
- Expected / observed: DOMAINS v1 called Blotto "non-potential (harmonic-ish)"; the meter reads α = 0.694 (budget 3, k = 3) and 0.711 (budget 4) — a genuine ~30% potential component survives normalisation despite the game being zero-sum. Zero-sum ⇏ harmonic-pure: the normalised Blotto game has real gradient structure (allocation dominance relations) alongside its circulation.
- Chase status: parked (consistent with theory; RPS by contrast reads exactly 1). Worth revisiting when the α > 0 anchor is used quantitatively: the bracket's right end sits at ~0.7, not 1.0.
- Resolution / follow-up: use RPS for the α = 1 extreme and Blotto for a *realistic* high-α strategic system; note in p1_instruments.

## F-0006 — The criticality wedge: potential games ESCAPE criticality at high λ; the supercritical frontier descends with α

- Date: 2026-08-08
- Instrument / experiment: `experiments/phase_map.py` (seed 20260808; 9 λ × 11 α × 5 games, median surface in `phase_map_surface.json`)
- Expected / observed: naively ρ(SB) ∝ λ (since S ∝ λ). Observed: **on α = 0 the median ρ is non-monotone** — 0.07 → 0.51 (λ = 1.7) → 0.0 (λ = 15). Mechanism (checked against theory, not just eyeballed): as λ → ∞ on a potential game, the principal branch concentrates on a strict equilibrium, C = diag(σ) − σσᵀ → 0 exponentially, so S = λC → 0 despite the λ factor — the meter escapes criticality through equilibrium concentration. At α = 1, mixed equilibria keep C bounded away from zero and ρ grows without bound (6.9 at λ = 15). In between, a **supercritical wedge** opens: the *median* game first crosses ρ = 1 at α = 0.5 (λ ≥ 8.5); a 0.2 fraction of games crosses already at α = 0.4; the frontier descends monotonically with α (λ_c ≈ 3 by α = 0.8). (Onset wording corrected per red-team: median-onset is 0.5, not 0.4.) **Caveat**: inside the wedge (ρ ≥ 1) the resolvent is near-singular, so ℛ medians there are magnitude-unreliable — direction only.
- Chase status: chasing. (1) Is the α = 0 peak location λ ≈ 1.7 tied to the payoff scale (scale = 2.0 here — test the folding); (2) is the wedge boundary a clean curve λ_c(α) (finer grid); (3) does the wedge boundary coincide with where within-level ℛ–EPR coupling degrades (F-0004's α ≈ 0.75 is suspiciously inside the wedge)?
- Resolution / follow-up: finer sweep along the frontier; p3_noneq's phase-map section; the Lab's live phase panel should draw the wedge.

## F-0007 — The response layer and the dissipation layer are distinct observables (H1 refuted, H2 confirmed)

- Date: 2026-08-08
- Instrument / experiment: `experiments/decoupling_mechanism.py` (paired design: the identical seeded families as `chain_comovement`, seed 20260808, λ = 1.2)
- Expected / observed: F-0004's working hypothesis — itself proposed *post hoc* to explain the observed decoupling, and tested here as a genuine prediction — predicted the numerator ‖χ−χᵀ‖ would keep tracking EPR at high α (only the ratio's denominator misbehaving). **Observed: H2 confirmed — at α = 0.95, ρ(ℛ, 1/‖χ+χᵀ‖) = 0.993, rising monotonically from ≈0 at low α: ℛ's within-level variation at high α is entirely the shrinking symmetric response. H1 refuted — the numerator ALSO decouples (ρ(num, EPR) = −0.37 at α = 0.95, from +0.77..0.86 at α ≤ 0.65).** No renormalisation of the response matrix recovers the dissipation ordering in the near-harmonic regime.
- Chase status: resolved as a structural fact, and it is the *better* result: χ^eq is a local derivative at the QRE point; EPR is a global functional of the stationary flux of the joint-profile chain. They co-vary exactly while a potential component modulates both (α ≤ 0.65) and part company when it vanishes. **ℛ remains the potentiality *test* (= 0 iff potential, exact at every λ); EPR remains the dissipation *gauge*; neither substitutes for the other across the harmonic regime.** Negative result for the corrected-meter idea, written down plainly.
- Resolution / follow-up: p1_instruments states the instrument-scope table (which meter answers which question, with the α-regime of validity); p3_noneq gets the mechanism section. Open: why num ~ 1/den within-level at α = 0.95 (near-perfect rank agreement of numerator with inverse denominator) — likely overall χ-scale variation; check by conditioning on ‖χ‖_F.

## F-0008 — First real-data reading: DAM at-null; RTM escapes its null on the LOW side (REVISED after red-team)

- Date: 2026-08-11 (revised same day — the original "certified null" claim was retracted on red-team review)
- Instrument / experiment: `kld_epr` on discretized/embedded price series via `experiments/electricity_reading.py` (unit domains.electricity)
- Config: `config/experiments/electricity_reading.yaml` (seed 20260811; SP15; DAM hourly 35d n=840; RTM 5-min 14d n=4032; 6 bins; 200 surrogates each of FT and AAFT)
- Expected / observed: expected the diurnal ramp to register as irreversibility. Observed, with the CORRECT null (amplitude-adjusted FT — plain FT Gaussianizes a kurtosis-132 marginal and was rejected by red-team): **DAM hourly prices sit INSIDE the AAFT null band** (0.0447 vs band [0.034, 0.075]) — consistent with a linear time-reversible process at this n/resolution/embedding. **RTM 5-minute prices escape the null band on the LOW side** (0.0474 vs AAFT q01 = 0.239, 5–6× below the median): NOT a detection and NOT a certified null — the linear+marginal surrogate class cannot reproduce the data's Δ-sign persistence (real RTM prices flip direction far less than any linear process with their spectrum+marginal), so no adequate null exists within the classes tried.
- Also established (proven in unit tests, then observed on data to 4 decimals): price-VALUE discretization is structurally blind to loop irreversibility; plain-shuffle nulls fail twice (persistence; iid-embedding asymmetry ~0.15 nats); plain-FT nulls fail on heavy tails. Three wrong nulls caught, each now a permanent guard.
- Chase status: chasing. (1) Constrained surrogates that match Δ-sign persistence (IAAFT with iteration, Markov-matched or block surrogates) to get a bracketing null for RTM; (2) the RTM persistence anomaly itself — WHY do real-time prices ramp so much more persistently than their linear image? (candidate: dispatch inertia / ramp constraints — possibly the irreversibility hiding in a statistic the KLD at k=1 cannot see); (3) finer bins, longer windows, spike-conditioned reads.
- Resolution / follow-up: the honest verdicts are "DAM: at-null", "RTM: no detection, null construction open". p3_noneq data section blocked on chase item (1).

## F-0009 — First positive real-data detection: DAM hourly prices violate pair-level detailed balance (diurnal-loop signature)

- Date: 2026-08-11
- Instrument / experiment: third null class added to `experiments/electricity_reading.py` — the REVERSIBILIZED-MARKOV surrogate (fit the embedded chain's pair counts, symmetrize the flux (C+Cᵀ)/2, simulate the detailed-balance chain with identical persistence, re-read)
- Config: as F-0008 (seed 20260811, 200 surrogates per class)
- Expected / observed: F-0008's chase item 1 asked for a null that brackets persistence. Observed: **DAM hourly reading 0.0447 nats/h EXCEEDS the reversible-Markov null q99 = 0.0291 (markov_detected=1)** — the observed pair-flux asymmetry of the (price-bin, Δ-sign) chain is beyond reversible-chain chance at matched persistence: a detection of the diurnal loop's irreversibility at hourly resolution, previously masked by the spectral nulls' inflated flip-rate bias. **RTM 5-min is genuinely at-null in the matched class** (0.0474 inside [0.002, 0.079]) — at that sampling rate dwell dominates and k=1 sees nothing; k>1 or coarser Δt is the follow-up there.
- Caveats (stated before anyone else states them): the Markov null is a parametric bootstrap under the reversibility restriction of the FITTED first-order chain — it tests pair-level (k=1) detailed balance of the embedding, not full-process reversibility; and this finding has NOT yet had its own adversarial review (the unit gate closed on F-0008's revised wording; F-0009 is a chase resolution logged pending red-team).
- Chase status: chasing — (1) independent red-team verification of the Markov-null construction; (2) RTM at k=2 / coarser Δt; (3) effect size in physical terms (nats/day of the diurnal cycle ≈ 1.1) for p3_noneq's data section.
- Resolution / follow-up: reconciles the three null classes into one picture: FT (wrong marginal), AAFT (wrong flip rate), reversibilized-Markov (matched persistence — the sharp test). p3_noneq data section can now be drafted around DAM-detected / RTM-open.

### F-0009 addendum — red-team verification + mandatory temporal stratification (same day)

- **Red-team verdict: granted-conditional.** Verified numerically: the reversibilized chain satisfies detailed balance exactly (violations 0 at 1e-10; stationarity residual 3e-17); false-positive rate on truly reversible data 1/30 (consistent with α=0.01); detection robust to surrogate seeds (5/5), n_price_bins ∈ {2,3,4} (3/3), tie-breaking flip (stat moves 0.9%), and Bonferroni across 2 markets × 3 null classes (empirical p < 0.01, q99 at α/2 still exceeded); higher-order (order-2) reversible structure does not explain the pair statistic.
- **Blocking condition (subsample fragility) resolved via weekly stratification** (5 × 7d blocks, per-block nulls): every week's statistic exceeds its null MEDIAN (ratios 1.63, 1.52, 3.03, 9.10, 4.17 — the signal has consistent sign throughout), but per-week significance concentrates in the second half (weeks 4–5: p ≤ 0.005; week 3: 0.035; weeks 1–2 underpowered at n=168). Leave-one-week-out: detection survives removal of weeks 1, 2, or 4 but not 3 or 5. **Explicit acknowledgment per red-team condition (B): the July-2026 detection is statistically driven by the high-ramp second half of the window — physically sensible (irreversibility concentrates where the diurnal drive is strongest, summer scarcity ramps) and stated as such wherever the number is used.**
- **Garden-of-forking-paths note (red-team O-2)**: the Markov null was chosen after two spectral nulls failed; mitigation is the prominently-reported empirical p-value (<0.01) and this documented selection history.
- p3_noneq data section may now cite F-0009 with the concentration caveat verbatim.

### F-0009 addendum 2 — RTM k=2 probe (chase item closed)

- RTM 5-min vs its reversibilized-Markov null at k=2: stat 0.0833 vs null [med 0.0591, q99 0.1176] — still no detection (and k=1 re-confirmed: 0.0474 vs q99 0.1026). The RTM at-null verdict is robust to block order at this resolution; remaining RTM avenue is coarser Δt aggregation (30–60 min), queued low-priority.

### F-0009 addendum 3 — bidding-oracle pipeline: stylised model REJECTED by the data

- The electricity plugin's five-object contract is complete (uniform-price BiddingOracle, offer-ladder grid, exact-linear offer-shift conjugate field, CAISO loader, learn spec; α of the calibrated auction is mixed, not an anchor — unit-tested). The conditional λ̂ pipeline (match QRE clearing-price dispersion to observed) returned the RIGHT kind of answer on July-2026 SP15: **model rejected** — the stylised duopoly's dispersion ceiling (10.4 $/MWh, its λ→0 maximum) sits 40% below the observed spike-driven std (16.8), so no λ is reported (`electricity_lambda.json`, model_rejected=1, λ̂=null). Scarcity spikes exceed what 2-agent undercutting can generate; a richer supply model (capacity constraints / N>2 / demand uncertainty) is the follow-up before any market-λ claim.

## F-0010 — The F-0004 reversal is λ-dependent: universal COLLAPSE, conditional SIGN FLIP

- Date: 2026-08-11
- Instrument / experiment: `experiments/frontier.py` crossover sweep (unit science.frontier; seed 20260811; α∈{0.55..0.95} × λ∈{0.8,1.2,2.0} × 60 games at 3×3, plus 4×4 at λ=1.2)
- Expected / observed: the initial criterion (ex-ante hypothesis, written before the run but not externally locked — not 'pre-registered' in the strict sense) expected F-0004's negative within-level ρ(EPR,ℛ) at α=0.95 in ≥3 of 4 conditions — **it failed (2/4)**, and the failure is the finding. What IS universal: the COLLAPSE of coupling at α=0.95 (from ~0.90 at α≤0.65 to ≤0.25 in every condition, both sizes). The SIGN goes negative only as λ grows: +0.25 (λ=0.8) → +0.03 (λ=1.2) → −0.23 (λ=2.0); and −0.26 at 4×4/λ=1.2. F-0004's −0.355 (different seeds, 100 games) sits within this gradient.
- Reading: the structural fact (F-0007's local-derivative-vs-global-flux decoupling) shows up first as decorrelation; the negative sign is λ-DEPENDENT across the three λ tested (each individual sign within ~2 null-SD at n=60 — suggestive, not established) — plausibly the supercritical wedge's doing (λ=2 at α=0.95 is deep inside it, where ℛ magnitudes are resolvent-dominated). The 4×4 negativity at λ=1.2 suggests size also pushes toward reversal.
- Criterion revision (documented, not silent): the artifact's pass criterion is revised from "reversal in ≥3/4 conditions" to "collapse (corr < 0.35 at α=0.95) in ALL conditions, with the sign pattern recorded as data"; the original criterion and its failure are in this entry and the artifact notes.
- Chase status: parked with a sharp question for p3: is sign(corr at α=0.95) a function of distance INTO the wedge (ρ−1) rather than of λ per se? One regression on existing per-game data would answer it.
- Resolution / follow-up: p3_noneq decoupling section gains the λ-gradient; C1's crossover row in claims.md updated.

### F-0010 addendum — the wedge-depth hypothesis REFUTED (the last open chase of this run)

- Date: 2026-08-11
- Test: pooled per-game (ρ(SB), EPR, ℛ) at α=0.95 across λ∈{0.8,1.2,2.0}, 60 games each (same seeds as the crossover sweep).
- Observed: **0 of 180 games are supercritical** at these λ (consistent with the measured frontier: λ_c(0.80)≈3.0 descending ⇒ λ_c(0.95) still above 2). Yet the within-level sign flips with λ across exactly these conditions (+0.25 → +0.03 → −0.23). Conclusion: the F-0004/F-0010 decoupling and its λ-dependent sign occur ENTIRELY OUTSIDE the supercritical wedge — wedge depth is not the driver, and F-0006's "suspicious coincidence" (decoupling regime sitting inside the wedge on the coarse map) was a coincidence of the coarse grid's λ range, not a mechanism. The remaining candidate driver is λ-dependent equilibrium concentration itself (C shrinking, reweighting the χ scale) — a p3 discussion point, not a claim.
- Methods note: a pooled-across-λ Spearman (+0.89) was computed and DISCARDED as λ-confounded (both meters grow with λ) — the same confound F-0004 taught us marginally over α; recorded so nobody re-runs the wrong cut.

## F-0011 — The reciprocity meter's first empirical read: ℛ ≈ 0.001 on real pass-through data, exactly where theory demands zero

- Date: 2026-08-11
- Instrument / experiment: `experiments/pricing_reading.py` (unit domains.pricing) on the PI's HF Dominick's canned-soup panel (`qbz506/dreamprice-dominicks-cso`, CC-BY-NC-4.0, Kilts Center origin; ~500K store×UPC×week rows, a random subsample handled gap-tolerantly via brand-level store-week indices)
- Design + ex-ante prediction (in the config BEFORE the run): cross-brand wholesale-cost pass-through between Campbell and Progresso, regular prices only, two-way (store, week) demeaning, cluster bootstrap over 86 stores. Because ONE retailer prices both brands (category management), a single-objective optimizer must respond symmetrically — prediction: small ℛ, asymmetry CI covering zero.
- Observed: own pass-through 1.07 / 0.97 (textbook); cross terms 0.0028 / 0.0005; asymmetry CI [−0.005, +0.010] ∋ 0; **ℛ_empirical = 0.0011 [0.00005, 0.0050]**. The prediction is confirmed: real category pricing reads potential-like, and N2's "ℛ estimable from pass-through asymmetry without payoffs" is now an executed measurement, not a promise. C2's first anchor: ℛ_emp sits at the potential end of the synthetic bracket (Sioux 0 / Blotto 0.12 / RPS 0.69), consistent rather than orders off.
- Companion scan (same artifact set): Edgeworth-cycle test on 30 stores' weekly category price indices vs the reversibilized-Markov null — **0/30 detections** (expected false positives 0.3): weekly category indices are at-null; the Maskin–Tirole signature, if present, lives at item-level/finer timing (and the HF subsample's week gaps push toward reversibility — detections would have been conservative, non-detections are not).
- Bug caught before review: the first run's χ row ordering swapped own/cross slots (giving a spurious ℛ = 0.54); caught by economic sanity inspection (both prices "loading on one brand's cost"), fixed, rerun — recorded here so the artifact history is interpretable.
- Red-team (2026-08-11, granted after one condition): the mechanical-correlation attack (COST derived from PRICE) was tested numerically and CLEARED — residual price–cost correlation after demeaning is 0.94–0.96, margin CV 0.26–0.35 (identification comes from idiosyncratic margin movement, not the accounting identity), and the cross terms use the OTHER brand's cost so they are never mechanically linked. Blocking condition — the bootstrap demeaned once and resampled demeaned rows — closed by re-implementing the standard protocol (re-demean within every resample, duplicated stores relabelled as distinct clusters): ℛ CI moved [0.0001, 0.0050] → [0.00005, 0.0050], asymmetry CI unchanged to 4 decimals. Also on record: the "ex-ante" config comment is same-session, not externally locked (the honest wording is "stated in config before the run"), and single-retailer symmetry is exact only under a symmetric demand Jacobian — the observed near-zero cross terms make ℛ ≈ 0 robust to that caveat. Edgeworth series lag-1 autocorrelation ≈ 0.57, so the at-null verdict is about persistent price dynamics, not basket-composition noise.
- Chase status: parked with two sharp follow-ups: (1) the TRUE multi-retailer reciprocity question needs cross-CHAIN data (Dominick's vs competitors) — this dataset cannot ask it; (2) item-level Edgeworth at finer resolution once a non-subsampled panel slice is used.
- Resolution / follow-up: p1's instruments section gains the empirical ℛ row; C2 updated.

## F-0012 — The driving cost inverts across the family: potential games pay per change, harmonic games pay per unit time

- Date: 2026-08-12
- Instrument / experiment: `experiments/protocol_reading.py` (unit thermo.protocols) — Hatano–Sasa housekeeping/excess split + stepwise λ-quench (0.5→3.0) across the α family (coordination↔matching-pennies mix, exact dense 2×2 joint chains).
- Pre-registration status (stated honestly, red-team O-3 follow-through): the config with P1–P3 was WRITTEN before the run (P1 excess ⟨Y⟩ ~1/K under refinement; P2 housekeeping ≈ 0 at α=0, monotone in α, duration-linear; P3 ⟨e^{−Y}⟩ = 1 machine-precision), and a pre-registration commit was attempted before the experiment — but that commit ABORTED on a pre-commit hook failure and did not land; the red-team caught the gap (config staged, not committed, when results existed). So this run is file-mtime-ordered, not commit-ordered. The predictions were not adjusted post hoc, but the audit trail cannot prove that; recorded as a process failure, and the standing rule is upgraded: verify `git log` shows the config commit landed before running.
- Observed: **all three PASS** — refinement slopes −1.02/−0.94 (the 1/K law), housekeeping 0 → 0.72 → 3.39 → 8.00 → 12.38 nats across α with duration-linearity R² = 1.000 (burn rate 0.774 nats/time at α=0.95), max IFT error 0.0; sampled-path IFT CI [0.9997, 1.0002] ∋ 1 over 3000 exact-kernel trajectories.
- **The honest deviation**: P1's prose expected excess "the same order across α". Wrong — excess COLLAPSES with α (0.036 → 2.8×10⁻⁵, three orders): as the mix approaches matching pennies the NESS becomes λ-insensitive (uniform at every λ), so there is almost nothing to relax when λ moves. The refinement-law pass criterion was unaffected; the wording is corrected here rather than papered over.
- Reading: the cost of driving a strategic system inverts across the family. Potential games pay only for CHANGE (excess ∝ how coarsely you step λ; quasi-static driving is asymptotically free) and nothing to exist. Near-harmonic games pay almost nothing for change but a constant HOUSEKEEPING rent per unit time just to hold their NESS — at α=0.95 the housekeeping/excess ratio at the base protocol is ~4.4×10⁵. "You cannot drive a whirlpool slowly for free" — worse: the whirlpool charges rent whether you drive it or not.
- **Mechanism: OPEN (red-team O-2)**. The first-pass explanation — "the near-harmonic NESS is λ-insensitive, so there is nothing to relax" — was REFUTED by the red-team's probe: on an asymmetric α=0.95 mix whose NESS genuinely moves with λ (‖Δπ‖ ≈ 7×10⁻³ across the ramp), excess still collapses ~290×. The collapse is real; its driver is not yet identified (candidate: λ-dependent equilibrium concentration re-weighting the relaxation spectrum). Chase items: mechanism isolation, larger action spaces, asymmetric harmonic sources.
- Also on record (red-team O-1): the exact-transfer ⟨e^{−Y}⟩ = 1 output is a telescoping algebraic identity for stepwise protocols — it cannot catch kernel bugs; the correctness check is the independent sampled path. And the sampled check at α=0.95 has ⟨Y⟩ ~ 6×10⁻⁵, so its CI-brackets-1 verdict is low-power there; a moderate-α (0.5) sampled check with real power is in the artifact.
- Resolution / follow-up: p3 gains the protocol section; the split is the estimator-facing groundwork for Hatano–Sasa-style estimation on data.

## F-0013 — F-0012's mechanism resolved by the EFE campaign: the excess collapse is the shrinking NESS path length

- Date: 2026-08-12
- Instrument / experiment: `experiments/bayes_reading.py` campaign (unit estimate.bayes) — the first EFE auto-research run: four pre-declared quantitative hypotheses (potential-scale fold / NESS-sensitivity floor / spectral gap / quadratic strawman), probes = exact quench computations over 5 α × 4 protocols, selection by BALD expected information gain, Gaussian obs model σ = 0.15 dex.
- Pre-registered (config commit VERIFIED landed before the run — the upgraded O-3 rule): NO winner predicted; whichever hypothesis wins must pass an absolute adequacy guard, else "all inadequate" is the verdict.
- Observed: the campaign selected the single most discriminating probe (α = 0.95, base protocol; EFE 1.19 nats) and resolved in ONE round: **ness_floor at belief 0.996**. Held-out validation (added after the one-probe stop; recorded in config): the winner predicts all 19 unconsumed probes with median residual 0.002 dex, max 0.043 dex — far inside the 0.30-dex adequacy bound. σ-sensitivity re-run (0.30 dex) agrees.
- Reading: excess quench dissipation ≈ excess(α=0, same protocol) × F_α/F_0 with F = Σ_k D(π_{k−1}‖π_k) — the collapse of F-0012 is the shrinking thermodynamic (Fisher) path length of the stationary state along the λ-ramp. The red-team's refutation of the "λ-insensitive NESS" wording is refined, not contradicted: what matters is not whether π moves but HOW MUCH divergence the ramp path accumulates — and that path length collapses with α even when π visibly moves. scale_fold and spectral_gap lose outright (their α=0.95 predictions miss by ≫ σ).
- Also in the artifact: the estimator side. The recovery study FAILED its pre-registered coverage floor on the first run (34/50 at λ* = 1.8) — chased to a real bug: a posterior with < ~6 effective grid points quantises its credible interval to ~2 grid steps and undercovers (78%). Fixed by raising the resolution bar and making `refined_posterior` auto-refine; coverage after fix 48/46/48 per λ*. The estimator's own calibration study caught its own interval bug — recorded as the estimate.bayes gate's origin story.
- Chase status: CLOSED with a scope correction (red-team A2, validated 2026-08-12). 3×3 family (coordination-3 ↔ RPS): excess/F flat in α to 0.1% at the base protocol — the formula generalises. BUT the red-team's near-tautology point is right and now owned: in the WELL-RELAXED regime (τ ≳ 1) ⟨Y⟩ → Σ_k D(π_{k−1}‖π_k) is an understood analytic limit (excess/F ≈ 1.03 uniformly), so there the 'mechanism' is the statement that the campaign's probes sat in that regime and the floor formula is exact-in-the-limit. At FAST protocols (τ = 0.1) the lag term dominates (excess/F ≈ 5) and drifts 12% with α — ness_floor is NOT adequate there, and F-0013's formula is scoped to well-relaxed quenches. The fast regime's α-dependence is the genuinely open remainder.
- Resolution / follow-up: F-0012's "mechanism OPEN" is now closed by this entry; p3 discussion gains the path-length mechanism; the EFE loop is a library capability (strataq.estimate.bayes.run_campaign).

## F-0014 — Fast quenches: dissipation bracketed by two exact limits; the guards caught the machine being confidently wrong first

- Date: 2026-08-12
- Instrument / experiment: `experiments/fast_quench_campaign.py` (unit science.quench_regimes) — second EFE campaign, targeting the regime where F-0013's floor formula fails. Pre-registered (commit 30c6cd1 verified landed before the run): four hypotheses, no winner declared, adequacy + held-out + σ-sensitivity guards.
- **Run 1, on the record**: the campaign consumed ONE probe (α=0.95, τ=0.1), concentrated to belief 1.0 on slow_floor — and the pre-registered held-out guard REFUSED it (median residual 0.60 dex over 29 probes): verdict `winner_failed_validation`. Lesson institutionalised: belief concentration under a Gaussian obs model can be confidently wrong about the rest of the probe space; `run_campaign` gained a `min_probes` parameter (tested), and the config records the post-hoc addition.
- **Run 2 (min_probes=6)**: `gap_interpolation` at belief 0.9999 — excess ≈ F + (D_frozen − F)·e^{−gτ} with F the slow floor, D_frozen = D(π_start‖π_end) the τ→0 telescoping limit, g the mid-ramp spectral gap. Adequate on consumed probes (median 0.20 dex), VALIDATED on all 24 held-out probes (median 0.089 dex), σ-sensitivity agrees. The EFE selector's consumption pattern is itself informative: after the α=0.95 probe it hammered α=0 across τ — the potential end is where the hypotheses disagree most about relaxation.
- Reading (as corrected by red-team round 2): quench dissipation is bracketed by the Fisher path-length floor (τ→∞) and the frozen divergence D(π_start‖π_end) — the latter exact AT τ = 0 (telescoping) but with a NON-UNIFORM approach: when the NESS path is loop-like (π_end ≈ π_start while intermediate πs differ, e.g. α = 0.95 with λ_end = 6), D_frozen → 0 yet small-but-finite-τ excess stays order-one, and the interpolation formula fails by ~1 dex there. Where the π-path is monotone the single-gap crossover holds to ~0.1 dex (held-out median 0.089).
- Caveats (full accounting, not held-out-only): consumed-probe max residual 1.06 dex — the α=0.95, λ_end=6 loop-path case above, which the adequacy MEDIAN averaged over; held-out max 0.46 dex (τ=0.3 crossover band, α ≤ 0.75 — first-order formula, as expected). Formula scope: monotone π-paths, moderate ramp amplitude; midpoint-gap convention is σ-insensitive but ~9% worse than gap-at-λ_start (red-team probe) — convention recorded, not tuned. min_probes is a STOPPING gate, not validation — the held-out guard remains the validator; probes 1–5 of run 2 carried low EFE (the space had one dominant discriminator). 2×2 family, one ramp shape.
- Resolution / follow-up: closes F-0013's "fast regime open" item at first order; p3 §protocols can cite the bracket; multi-mode refinement parked as a chase item.

## F-0015 — The recursive lag model supersedes the global crossover; naive two-mode projection anti-helps

- Date: 2026-08-12
- Instrument / experiment: `experiments/quench_multimode_campaign.py` (unit science.quench_multimode) — third EFE campaign; pre-registered (commit 0cb2d4a verified landed), probe grid INCLUDING F-0014's failure cells; full-accounting grid table (every hypothesis × every probe) in the artifact.
- Observed: **recursive_1mode** (per-step single-gap recursion p_k = π_k + (p_{k−1} − π_k)e^{−g_k τ}, g_k the gap at the CURRENT λ_k, Y accumulated along the tracked p) at belief 1.0 after 6 probes; adequate + held-out validated (median 0.072 dex, max 0.36); σ-sensitivity agrees. Grid table: recursive_1mode dominates the incumbent on BOTH median (0.073 vs 0.114) and worst case (0.44 vs **1.11** — F-0014's loop-path catastrophe is fixed by construction, since the recursion follows the path instead of comparing endpoints).
- **Two-mode result, corrected on the record (red-team round 3)**: run 1's two-mode implementation was BUGGY (left/right eigenvector mix-up + truncated least squares silently dropping mass) — that version's anti-result is RETRACTED as an artifact. The FIXED implementation (full left-eigenbasis solve, conjugate pairs kept together, fast remainder dropped explicitly) still loses to one mode (0.19 vs 0.04 dex at the 2×2 test cell; 0.44 vs 0.20 at 3×3): with exact modal coefficients, truncating the fast remainder at each switch errs MORE than the 1-mode model's over-retention (uniform e^{−gτ} damping of the whole deviation). The honest statement is now about truncation, not non-normality.
- **Scope and geography (red-team conditions A/C)**: the incumbent's failure band is α ∈ [0.5, 1.0] at long ramps (not only the α ≥ 0.85 loop paths); the headline loop-path improvement is +0.67 dex at (α=0.95, τ=0.02, λ_end=6); recursive_1mode's own worst case 0.44 dex sits at α=0 (τ=0.3, λ_end=6 — the crossover band), a different cell from the incumbent's worst. 3×3 probes (6 cells, coordination-3 ↔ RPS) were ADDED to the grid after the red-team measured degradation on 27 states: recursive_1mode holds within adequacy there (0.198 dex at the α=0.95 long-ramp cell) but errors are systematically larger than on 2×2 — multi-mode structure is real on bigger chains, just not captured by naive truncation.
- Reading: the quench dissipation model is per-step and path-aware; F-0014's global bracket survives as the recursion's two limits.
- Chase status: parked at 0.44 dex worst-case (2×2) / ~0.36 (3×3 held-out max); the principled multi-mode treatment must handle the truncated remainder (e.g. damp it at a fast effective rate instead of dropping it) — recorded, not attempted.
- Resolution / follow-up: supersedes F-0014's interpolation as the working model; p3 §protocols updated; F-0014's guard story unchanged.

## F-0016 — OPEN: the IFT cannot self-calibrate the plug-in Hatano–Sasa estimator; the gap collapse is why holds never settle

- Date: 2026-08-12
- Instrument / experiment: unit thermo.hs_estimator (IN PROGRESS — no gate claimed). The data-facing plug-in: π̂_k from burned-in hold-window occupation, Ŷ accumulated at switches; registered criteria P1–P3 in config/experiments/hs_estimator.yaml (commit verified landed; the powerless α=0.5 regime swap and the covers-1→equivalence diagnostic escalation both recorded in-config).
- What the registered runs found, in order: (1) P3 FAILED — the covers-1 IFT diagnostic is non-monotone in τ (failure-to-reject flips by seed at the boundary); (2) the equivalence upgrade (CI ⊆ [0.95, 1.05], n=800) STILL false-passes at τ=1.0 where mean Ŷ is ~45% biased low — **the IFT diagnostic is structurally insufficient**: state-correlated plug-in errors cancel in ⟨e^{−Ŷ}⟩ but not in ⟨Ŷ⟩; (3) a within-window TV stationarity check is flat across τ (0.05–0.07) — because the drift is NOT the switch transient; (4) mechanism isolated numerically: the spectral gap COLLAPSES along the ramp (0.88 at λ=0.5 → 0.15 at λ=5.5; one relaxation time = 6.6 units), so late windows never settle at any tested τ — the F-0006 concentration effect biting the estimator.
- Status: the estimator (strataq.thermo.hs_estimator, tested for what it does) quotes calibrated CIs only in the regime where windows settle, and its current usable flag is known-insufficient — the docstring must not promise self-calibration until the per-window condition (effectively τ·gap_k ≳ 3, with gap_k estimable from within-window autocorrelation) replaces the IFT as the primary gate.
- **Round 2 (same day) — WITHHELD, and the unit stays OPEN**: the per-window relaxation gate looked complete (monotone boundary, admitted-hold coverage on the registered seed) but the red-team broke it four ways, all measured: (1) safety factor 4 was one-case calibration — the lag-N/4 underestimate reaches ~19x at alpha=0; (2) multi-seed coverage at the admitted hold is ~2/5 — residual plug-in bias beyond the CI (in-sample correlation between the switch state and pi_hat is the suspect); (3) the autocorrelation gate collapses at small n_trajectories (24x underestimate at n=1 — real data's regime); (4) the P1 re-scope was a criterion weakening, owned here: the sweep's PASS is void for certification. Module banner-marked EXPERIMENTAL; gate signoff NOT flipped. Redesign directions: split-sample pi_hat (decouple the switch state from the occupation estimate), explicit plug-in bias correction, game-adaptive safety from a multi-game calibration study, and a minimum-n analysis. Earlier same-day notes below record the path that got here:
- **Post-withheld chase (same day) — the coverage bias root-caused to a BUG, two hypotheses refuted on the way**: split-sample (leave-one-trajectory-out) pi_hat changes nothing (means shift 0.001 — the in-sample-correlation suspect REFUTED); relaxation-scaled burn-in helps only marginally (2/6 → 3/6 — the transient-contamination suspect mostly refuted); the actual cause: `sample_quench_states` emits windows only for holds AFTER each switch (lambda_1..lambda_K) with no pre-quench lambda_0 window, so `hs_y_estimate` silently computes K-1 of the K jump terms — the lambda_0 -> lambda_1 jump is MISSING, which is a systematic low bias of exactly the observed order. Fix for the redesign: prepend a stationary lambda_0 observation window (sampler + docstring contract + tests), THEN re-examine coverage before touching safety factors. The red-team's multi-seed objection (O-2) found a real defect neither diagnostic could see.
- **Fix landed (same day)**: `sample_quench_states` now prepends the stationary λ₀ window (contract documented); with all K jump terms the estimator is UNBIASED across seeds (means straddle exact; multi-seed mean within 0.01 of 0.089 — the bias story is CLOSED). The CI was moved to a trajectory bootstrap with π̂ re-estimated per resample — wider, but still under-covering ~1.7× (a common-mode π̂-noise component the within-sample bootstrap cannot see; per-seed coverage remains ~2-3/6 and the relaxation gate flickers at boundary holds). VARIANCE CONCERN RESOLVED (20-seed study, same day): CI coverage is 19/20 — the bootstrap is CALIBRATED, and the earlier '1.7x under-coverage' was a small-sample mirage (six seed-SDs compared against each other); a true-vs-estimated-pi decomposition confirms pi_hat noise is negligible next to switch-state sampling, which the CLT/bootstrap capture correctly. Estimator status: UNBIASED with CALIBRATED intervals. Remaining before re-registration: (1) the relaxation gate FLICKERS conservative at boundary holds (usable 4/20 at tau=24 — it refuses valid reads, never admits invalid ones; the per-seed tau_hat noise straddles the threshold; fix: a pooled or noise-aware threshold), (2) game-adaptive safety (the ~19x underestimate at alpha=0), (3) minimum-n analysis. GATE STABILISED (same day, escalations 3+4 in config): the relaxation threshold moved to tau_hat + 2 SE (4-way trajectory-split SE) — 0/10 flicker from the relaxation gate; the IFT companion's role changed to ANOMALY DETECTOR (flag when the CI EXCLUDES 1 — the equivalence form was pure power-starved flicker once certification moved to the relaxation gate). Final behaviour: tau=32 usable 10/10 with coverage 10/10; tau=24 is the honest marginal zone (5/10 admitted, every admitted read covers — conservative-only). The estimator is now unbiased, calibrated, and stable in its certified regime. ALPHA=0 RESOLVED by the existing gate (no game tuning needed): with the noise-aware lag-N/4 estimator the settled-hold underestimate at alpha=0 is only ~1.32x (the red-team's ~19x was the pre-fix machinery on unsettled windows); the gate demands tau >= ~86 there — correctly refusing everything short of the slow basin-hopping timescale — and at tau=110 admits 3/4 seeds with 3/3 coverage. Minimum-n: SE machinery is maximally cautious below n=8 and a thin-statistics warning now fires below n=100 (validated at n >= 200). CLOSED — GRANTED (fresh reviewer, 2026-08-12): v2 registration (commit ac1b0b0 landed pre-run) passed C1-C5 (one recorded C4 probe-point escalation out of the measured marginal zone; criterion structure unchanged; calibration 20/20); the reviewer's two blocking conditions (test the anomaly companion against a real non-stepwise input — it FIRES, CI [1.03, 1.10] excluding 1, now a permanent test; make the unit/scope contracts explicit) closed and verified in-repo; banner LIFTED with the certified scope declared. The unit is the 23rd green gate, and the arc — withheld, failure map, bug find, statistics vindicated, gates stabilised, certified — is the programme's fullest demonstration that the adversarial machinery converges.
- Superseded round-1 note (kept for the record): the per-window relaxation gate implemented from lag-N/4 categorical autocorrelation (data-side, no generator access); the lag-N/4 estimate itself underestimates slow modes ~25% (5.2 vs true 6.6), so the default safety factor is 4 — at safety 3 the τ=16 hold slipped through 58% biased (recorded escalation). Registered sweep now PASSES: boundary perfectly monotone (all τ ≤ 16 refused, τ = 24 usable), and the one admitted hold covers the exact ⟨Y⟩ (0.090 [0.053, 0.124] vs 0.089). The instrument's honest shape: on a ramp INTO concentration (gap 0.88 → 0.15), the settle requirement is τ ≳ 4 × 6.6 ≈ 26 time units per hold — data-side quench estimation is hardest exactly where λ concentrates, the F-0006 effect. p3 paragraph owed.

## F-0017 — What a real market read actually supports: one in-scope number (loop affinity ≈ 7 nats/day) and a monthly scan that cannot be interpreted at all

- Date: 2026-08-12 (THIRD revision, same day — two red-team rounds, each of which overturned the headline. Superseded versions are in git history; the arc is the point.)
- Instrument / experiment: `experiments/day_quench_reading.py` (unit domains.electricity.quench, R7) — the certified HS estimator machinery on 211 CAISO SP15 DAM day-pairs (2026-01-01→07-31), hour-blocks as holds, within-day phase embedding (6 states), registered D1–D4 (commit 0d09773 landed pre-run).
- **Revision 1 → 2 (red-team round 1, WITHHELD)**: the circular protocol was half-counted (closure adds ~3.7 nats — trajectories are now day-PAIRS carrying the wrap-around jump); 6 states is outside the certified 4-state scope (stated everywhere); serially-correlated days made the iid CI ~28% narrow (day-block bootstrap [6.22, 8.50] is now THE interval); the full-window "pass" was called regime-mixing cancellation, so the hierarchy was inverted to lead with a *seasonal-drift diagnosis*.
- **Revision 2 → 3 (red-team round 2, GRANTED-CONDITIONAL)**: the reviewer demanded a SHUFFLE CONTROL — re-read each month with day order randomised, which destroys temporal drift while preserving thinness and price distribution. Implemented as a permanent artifact metric (seeded, reproducible), and it demolishes the drift story: **0 of 7 months are drift-attributable.** Five anomalies SURVIVE the shuffle (so they are not drift), and May's flag FLIPS from clean to anomalous under a permutation that cannot change one physical property — direct evidence that at n ≈ 30 days the monthly read is sampling noise. Disagreement RESOLVED by the reviewer themselves: their one-off shuffle had suggested a Jan–Mar (persist) vs Apr/Jun (resolve) split, and on re-examination they traced it to RNG-state evolution (their draws came after the 200-iteration block bootstrap had consumed ~6000), confirming the seeded artifact control as authoritative. Both readings stay on the record.
- **What the unit therefore claims, and only this**: (1) the full-window, in-scope (n = 211 ≥ 200) read — the closed daily-loop affinity Ŷ_loop ≈ 7.0 nats/day, day-block bootstrap [6.22, 8.50] — quoted as DESCRIPTIVE, because 6 states extends rather than inherits the certification and because the IFT companion's open-chain identity does not constrain a closed cycle (its wide CI contains 1 vacuously); (2) the monthly scan is UNINFORMATIVE at this n and supports NO causal statement — not drift, not month-specific structure. The premise question ("is the trading day one repeated quench?") is not answered by this design, and saying so is the finding.
- Against F-0009's ~1.1 nats/day stationary pair-level EPR on the same market: different observables (the day's closed drive vs the standing loop); the ordering is sensible, the comparison qualitative.
- Chase status: parked with the honest requirement stated — answering the premise question needs either the small-n extension of the estimator's certification (so n ≈ 30 windows become quotable) or coarser windows with many more days; a 4-state (2-bin) embedding would also return the application inside certified scope. DST (the Mar UTC-boundary shift) remains an untested rival explanation for any residual seasonal pattern.
- Resolution / follow-up: gate claim and docs rewritten to this scope; the shuffle control is now permanent machinery, reusable by any future repeated-protocol claim on real data.

## F-0019 — Small-n REFUSED, and the binding constraint is not what anyone assumed: the *number* survives at n=20, the instrument's own *verdict* does not

- Date: 2026-08-12
- Instrument / experiment: `experiments/smalln_certification.py` (unit thermo.hs_estimator.smalln, R8) — can a month of market data (n ≈ 30 trajectories) be quoted with the certified HS estimator (scope n ≥ 200)? 15 (n × interval-method) cells × 20 seeds, each seed re-read under a trajectory-order permutation. Criteria S1 (coverage in [17,20]/20), S1b (median CI half-width ≤ 2× bootstrap SE), S2 (gate decision agrees with the n=400 reference ≥ 18/20), S3 (anomaly flag flips ≤ 1/20).
- **Process note that matters**: the design was red-teamed BEFORE any result existed. Its two blocking objections (coverage alone is gameable by width; "smallest of 15 cells" is a best-of-15 selection that passes by chance) and three must-address items were applied to the criteria while the first run was STOPPED mid-flight — its verdict was never produced or read. The amended config landed before the authoritative run.
- **Verdict: REFUSED at every tested n.** No cell passes all four criteria, so the n ≥ 200 floor stands and monthly windows remain unquotable *as self-certified reads*.
- **The mechanism, which is the actual finding**: interval COVERAGE is not the limiting factor. It holds at every n down to 20 (19–20/20) with sane width (~1.9× SE) for all three interval constructions — so the estimator's *number* is statistically fine on a month of data. What fails is the instrument's *decision machinery*: gate agreement with the reference is 10/20 at n = 20–30, 15 at 50, 14 at 100, 17 at 200; anomaly-flag flips under a physically-null permutation fall 4 → 6 → 5 → 3 → 1 across the same n. This mechanistically explains F-0017's observed flag instability at n ≈ 30 and quantifies its decay — the gate needs roughly an order of magnitude more trajectories than the estimate does.
- **The caveat is RESOLVED in favour of the existing floor (condition C-1)**: recomputing gate agreement against the TRUE settling status from the generator's exact spectral gaps — zero sampling noise, so the reference cannot be blamed — gives 19/20 at n = 200, which PASSES the 18 bar. The earlier 17/20 was measuring the finite-sample n = 400 comparator's own decision noise. Reading (a) is supported: the n ≥ 200 floor is solid, not marginal. Against the exact reference the small-n failure is unchanged (10/20 at n = 30, 15/20 at n = 50, 16/20 at n = 100), so the refusal does not depend on the choice of reference either.
- **And the headline mechanism is now LOCALISED — the refusal is implementation-bound, not fundamental (condition C-2)**: a WITHIN-GROUP permutation (shuffling only inside each `i::4` residue class, so the SE split's composition is preserved while the permutation stays physically null) collapses the flag flips from 6 → **0** at n = 30, 5 → 1 at n = 50, 3 → **0** at n = 100. Essentially all of the instability below n = 200 comes from the 4-way split reassigning trajectories between groups, not from the physics. So "the gate needs ~10× more trajectories than the estimate" is substantially an artifact of ONE implementation choice in `_relaxation_time`. The actionable consequence, now evidence-backed rather than speculative: a split-independent SE for the relaxation estimate (jackknife/leave-one-out over trajectories, or a bootstrap SE of τ̂) could plausibly stabilise the gate at n ≈ 30–50 and bring monthly market windows back into reach through better machinery instead of more data. That is R9, and it is registered as its own unit rather than claimed here.
- **Chase item REJECTED in writing (results red-team condition C-3), and the reasoning matters more than the idea**: I had floated that R7's monthly reads might become quotable by importing the settling verdict from the in-scope full-window read. That is illegitimate and is withdrawn. A joint verdict over 211 day-pairs does not imply that any particular 30-day subset settled: the full window can pass while January specifically did not, because the gate compares each hold's duration against a relaxation time estimated from the pooled trajectories — a subset with slower dynamics can hide inside a passing aggregate. Importing the verdict would assert exactly what the data cannot show. The only legitimate routes are (a) a separate small-n certification designed for n ≈ 30 (which R8 has now REFUSED under the current gate machinery), or (b) quoting monthly estimates with their intervals and an explicit "thin data: gate stability unvalidated at this n" warning and NO gate verdict at all. Route (b) is what the instrument honestly supports today.
- Resolution / follow-up: the refusal boundary is the deliverable; docs and the gate claim state it. `HSEstimate` now exposes `boot_se` so any caller can guard interval informativeness, and `interval_method` (percentile / bootstrap_t / t_widened) is selectable with `t_widened` warning that it is a heuristic comparator.

## F-0020 — The gate's order-dependence is FIXED and the small-n floor still does not move: two failure modes that R8 had fused into one

- Date: 2026-08-12
- Instrument / experiment: `experiments/gate_se_reading.py` (unit thermo.hs_estimator.gate_se, R9) + `experiments/gate_se_realdata.py` (real-data obligation). Four SE candidates for the relaxation gate — `split` (incumbent 4-way `i::4` trajectory split, baseline only), `jackknife` (leave-one-out over trajectories), `delta` (per-trajectory match-rate variance through dτ/dρ), `bootstrap` (trajectory resampling) — against registered criteria G1 (order-invariance, ≤ 1/20 flips), G2 (agreement with the EXACT spectral-gap settling status ≥ 18/20 at n ∈ {30,50,100}), G3 (≥ 19/20 at n = 200), G4 (absolute refusal power on unsettled holds), G5 (SE accuracy against an oracle SE, |median/oracle − 1| ≤ 0.35 on the worst window).
- **Process note**: G1–G5 were registered and verified landed before any code ran, and G5's window aggregation and oracle seed stream were pinned in a second commit while still blind to any result. A first authoritative invocation was stopped mid-flight and its output never read, to add Jeffreys intervals and the oracle's own relative SE — reporting only, no criterion touched.
- **Verdict: REFUSED. No candidate passes all five.** But the incumbent fails too, on three of the five, so this is not a defence of the status quo.

| method | G1 flips (30/50/100) | G2 agree (30/50/100) | G3 (n=200) | G4 | G5 worst-window dev |
|---|---|---|---|---|---|
| `split` | **7 / 6 / 0** | 9 / 15 / 20 | 20 ✓ | 60/60 ✓ | .437 / .488 / .449 ✗ |
| `jackknife` | 0 / 0 / 0 ✓ | 12 / 16 / 15 | 19 ✓ | 60/60 ✓ | .321 / .433 / .519 ✗ |
| `delta` | 0 / 0 / 0 ✓ | 3 / 2 / 6 | 4 ✗ | 60/60 ✓ | ~1e8 ✗ |
| `bootstrap` | 0 / 0 / 0 ✓ | **12** / 18 / 19 | 20 ✓ | 60/60 ✓ | **.181 / .197 / .285 ✓** |

- **G1 is solved outright.** All three alternatives give ZERO flips at every n, against the incumbent's 7/6/0. R8's C-2 prediction was correct: the flag instability was the `i::4` split reassigning trajectories, and removing the split removes it. For `jackknife` and `delta` this is a theorem (the set of leave-one-out replicates is permutation-invariant), so it is arithmetic rather than evidence — which is why the real-data check below exists. For `bootstrap`, whose invariance holds only in distribution, 0/20 is a genuine measurement.
- **G5 discriminates exactly as intended, and the winner is `bootstrap`.** The criterion existed to stop a candidate passing by *shrinking* the SE rather than making it accurate; measured against an oracle SE from 40 independent draws (its own relative SE 0.113), `bootstrap` sits 0.18–0.29 from truth versus the incumbent's 0.44–0.49 and `jackknife`'s 0.32–0.52. `delta` fails catastrophically on ONE window (median deviation 0.13, worst ~1e8): its gradient lag·dt/(ρ ln²ρ) explodes when ρ reaches its clip floor. `delta` is dead on two independent grounds — that fragility and a ~3× overstatement at n=30 from dropping the π̂ term that partly cancels match-rate noise.
- **G4 is clean for every candidate: 60/60 refusals at n = 30 and n = 100.** Nothing bought agreement by breaking the gate — the guard registered against exactly that did its job and found nothing to catch.
- **G2 fails at n = 30 for all four candidates, and this is the finding.** `bootstrap` misses on that single cell alone (12/20 against an 18 bar) with every other criterion passed. The mechanism: the truth here is *settled* (τ = 32 against a 26.37 requirement), and the gate refuses whenever 4·(τ̂ + 2·SE) > 32. The oracle shows τ̂'s own across-seed SD is 0.967 at n = 30 — roughly 35–40% of τ̂ — so in ~8 of 20 seeds τ̂ lands high enough to refuse a genuinely settled hold. **An accurate SE reports that variance faithfully and therefore must refuse.** The binding constraint is the variance of the relaxation-time ESTIMATE, not the error bar on it.
- **Partial retraction of my own extrapolation in F-0019.** That finding said a split-independent SE "could plausibly stabilise the gate at n ≈ 30–50 and bring monthly market windows back into reach through better machinery instead of more data". Half of that is confirmed and half is refuted: stability is delivered completely (0 flips everywhere), quotability is not (agreement at n = 30 improves only 9 → 12 of 20). R8 had fused two separable failure modes — flag *stability*, which was implementation-bound and is now fixed, and gate *accuracy* at small n, which is a real property of the τ̂ estimator. The next lever is a lower-variance relaxation-time estimator (multi-lag fit, or pooling across windows), not the SE.
- **Real data (R7's CAISO day-pair panel, flip rate over 20 null day-order shuffles per month)**: `split` 0.214 (30/140) → **0.050 (7/140)** for all three alternatives. Month 06 collapses 17/20 → 0/20; months 01–04 go 1–3/20 → 0/20. The SE method also MOVES ADMISSION: month 07 is refused under `split` and admitted under all three alternatives, with the estimate provably identical (full panel mean_y = 7.0000 for every method). F-0017's per-month refusals were therefore not method-invariant.
- **A SECOND order-dependence, outside everything R9 touched.** The residual 7/140 is entirely month 05 and is IDENTICAL under all four methods, which localises it away from the SE machinery: `hs_y_estimate`'s CI bootstrap uses `default_rng(0)` with index resampling, so permuting trajectories changes each resample's composition. Verified directly — on month 05 `mean_y` = 6.152647 to six decimals under every permutation while the IFT interval's upper bound swings 0.970–1.246 and the ANOMALY flag flips with it. Reseeding is NOT the fix: the flag is a hard boolean thresholded on a Monte-Carlo interval whose bound sits at the threshold, so the noise must be reported (margin, or a third indeterminate state) or driven far below the threshold distance. Captured as a characterisation test so the eventual fix cannot land silently.
- **Registered-rule gap, flagged rather than resolved unilaterally.** The decision rule says the default stays `split` if no candidate passes — but it did not anticipate the INCUMBENT failing three criteria while `bootstrap` fails one. Keeping a dominated default is defensible only on continuity grounds. Default left at `split` per the letter of the registration; the recommendation to adopt `bootstrap` is put to the red-team with the evidence rather than taken here.
- Chase status: chasing (R10 — the CI/IFT interval's order-dependence and its thresholded-boolean problem; and a lower-variance τ̂ as the actual route to small-n agreement).
- Resolution / follow-up: `relaxation_gate` is now a public standalone instrument with `se_method` and an exposed `se_sigma`; the refactor to sufficient statistics makes the leave-one-out jackknife O(nT) instead of O(n²T) and was verified bit-identical to the original `split` (max |Δτ̂| = max |ΔSE| = 0.0), so no previously recorded verdict moved. Artifacts: `gate_se_read.json`, `gate_se_realdata.json`.

## F-0017 — CORRECTION (2026-08-12, from R9)

The finding's substance stands: monthly anomaly flags on the CAISO panel are unreliable at n ≈ 30. Two specific claims in it do not, and are corrected here rather than edited away.

1. **"1 of 7 months UNSTABLE" carried unstated sampling error.** The label came from a SINGLE day-order permutation per month. A different draw gives 0 of 7. Measured properly as a flip rate over 20 shuffles per month, the incumbent's rate is 0.214 (30/140) — the instability is real and larger than one month, but the *count* was not a reproducible statistic. R9's first version of the same diagnostic inherited the same flaw and had to be rebuilt.
2. **The per-month refusals were not method-invariant.** Month 07 is refused under the incumbent SE and admitted under all three order-invariant candidates, with the estimate unchanged. The refusal reasons published in F-0017 were partly an artifact of the SE choice, not properties of the data.

Neither correction makes any month quotable: R8's C-3 rejection stands, and F-0020 shows small-n gate agreement is still 12/20 at best.

## F-0021 — The relaxation-time estimator silently degenerates on fast-mixing windows: one root cause behind three different SE failures

- Date: 2026-08-12
- Instrument / experiment: signed per-window diagnostic run while resolving the R9 red-team's must-address condition 1 (unit thermo.hs_estimator.gate_se). Registered criteria were not involved — this is a mechanism investigation of an anomaly the review flagged.
- **The anomaly**: `jackknife`'s G5 deviation *worsened* monotonically with n (0.321 → 0.433 → 0.519 → 0.654 at n = 30/50/100/200), which is backwards for a consistent estimator. G5 as registered stored only |ratio − 1|, so the direction was not recoverable from the artifact — a real defect in my own criterion, since a candidate that dangerously UNDERSTATES uncertainty and one that harmlessly overstates it score identically.
- **What the signed ratios show**: the failure is not diffuse, it is one window. Ratio of the MEDIAN SE (across seeds) to the oracle SE on the pre-quench λ₀ window: `jackknife` 0.32 at n = 30 → **0.15 at n = 200** against `bootstrap`'s 0.76 — an understatement of the uncertainty, the dangerous direction, getting worse with more data. **Stated precisely, because my first phrasing of this was sloppier than the data**: that is a ratio of medians, NOT a per-seed effect. Measured per seed, the jackknife/bootstrap SE ratio on window 0 has median 0.78 (n = 30) and 0.89 (n = 200) with only ~60% of seeds below 1 — so there is no reliable 6.7× per-seed understatement. What drags the median SE down is INTERMITTENT TOTAL COLLAPSE, now measured exactly: the window-0 SE is **exactly 0.000** on **6 of 20 seeds at n = 30 and 3 of 20 at n = 200** for `jackknife`, and on 2/20 and 1/20 for `split` — while `bootstrap` collapses on **0 of 20 at both n**. An SE of exactly zero is a harder failure than a biased one: a gate handed SE = 0 believes it knows τ̂ perfectly and drops its own noise margin entirely. That is the single cleanest reason to prefer `bootstrap`, and it is the claim the characterisation tests encode. `split` also understates on windows 0–2 (0.55–0.70). `bootstrap` sits at 0.76–0.98 there and errs high (1.59) where it errs at all.
- **The root cause, and it is upstream of every SE**: the gate estimates the autocorrelation at lag N/4. With 800 samples and dt = 0.04 that is a lag *time* of 8.0, against a true relaxation time of ~6.6 — so on the low-λ windows the correlation has already decayed into noise. Measured ρ at lag N/4: **−0.0185 and −0.0033 on windows 0–1 at n = 30 (both CLIPPED to the 1e-12 floor)**, and +0.0022 / +0.0088 / +0.0051 on windows 0–2 at n = 200. τ̂ = −lag·dt/ln ρ is therefore not measuring a relaxation time on those windows at all: it is reading noise about zero, and when ρ clips it returns a fixed floor of 0.29.
- **Three symptoms, one cause.** Every SE that relies on local sensitivity to ρ fails in that regime, in a way that looks method-specific but is not: `delta` EXPLODES (its gradient lag·dt/(ρ ln²ρ) divides by ρ → the ~1e8 deviation), `jackknife` COLLAPSES (clipped leave-one-out replicates are all pinned at the same floor, so the estimated variance goes to zero — and the deficit grows with n because the 1/n perturbation shrinks while the curvature does not), `split` partially understates. `bootstrap` survives because with-replacement resampling perturbs the statistic far enough to escape the flat/clipped region.
- **The consequence is larger than the SE question.** If τ̂ is a clip-floor artifact on the fast-mixing windows, the gate's threshold there is meaningless — and it happens to be *permissive* (τ̂ pinned tiny → the hold passes easily). The relaxation gate is effectively only testing the high-λ windows. It does not report this; it degenerates silently. This also explains why the aggregate G2 mechanism in F-0020 over-predicted the refusal rate (~81% predicted vs ~40% observed, the red-team's note): the per-window behaviour is heterogeneous, with windows 0–2 degenerate-and-permissive and windows 3–5 carrying the actual decision.
- **This sharpens R10 from "lower-variance τ̂" to something more specific**: the estimator needs (a) an ADAPTIVE lag chosen so ρ stays in a measurable range rather than a fixed N/4, and (b) an explicit REFUSAL when ρ is statistically indistinguishable from zero, instead of silently returning a floor. A gate that cannot measure a window must say so — the same principle the whole instrument was rebuilt on in F-0016.
- Chase status: chasing (R10, now with a specific design rather than a direction).
- Resolution / follow-up: recorded as the resolution of the R9 review's must-address condition 1 — a mechanism, not a deferred chase item. `bootstrap` is now the DOCUMENTED RECOMMENDATION in the docstring and reference docs (it is the only candidate that neither explodes nor collapses in the degenerate regime), while the default stays `split` for reproducibility of every recorded R7/R8 verdict; R10 carries the default flip together with its downstream re-run obligation.

## F-0022 — K2-T1 (finite size) is INDETERMINATE: the criterion was substituted after two runs had been launched, the deciding statistic's CI crosses its bar, and the two-coordinate claim rests instead on the ceiling criterion, which is robust

- Date: 2026-08-12; **corrected 2026-08-13** after the R11 red-team review returned WITHHELD on objections O-1 (substituted pre-registration) and O-2 (no interval on the deciding statistic). The earlier version of this finding recorded K2-T1 as SURVIVES. **That headline was wrong and is withdrawn here, not softened.** Everything the artifact does support is kept and foregrounded below.
- Instrument / experiment: `experiments/plane_robustness.py` (unit science.plane, R11). Two pre-registered kill-shots on the programme's flagship claim that response asymmetry ℛ (local derivative, χ^eq = (I − SB)⁻¹S) and Schnakenberg EPR (global stationary-flux functional of the Glauber chain) are independent coordinates: **K2** the collapse is a finite-size artefact of m = 3; **K3** the collapse is an artefact of the damped fixed-point solver. N = 2 throughout, λ = 1.2, scale 2.0, 200 games per main cell, percentile bootstrap 2000 resamples on every ρ_S.

### The pre-registration was substituted (red-team O-1), and the substitution changes the verdict

- **There are TWO registrations of this unit's K2 kill-shot, not one.** The first, `config/experiments/plane_finite_size.yaml`, was committed at **a6533e7 (2026-08-12 23:02:25)** for the same unit and the same kill-shot. `experiments/plane_finite_size.py` was written at **23:04:39** and **executed at 23:04:49** (`experiments/__pycache__/plane_finite_size.cpython-311.pyc`), and a second run was launched at **23:17:47** (`benchmarks/results/plane_finite_size.resolved.yaml`). Only afterwards, at **23:26:36**, was the replacement `config/experiments/plane_robustness.yaml` committed at **b89d5df**. **No artifact from the first registration exists** — `benchmarks/results/plane_finite_size.json` was never written — so the record of those two runs is the resolved config and the bytecode timestamp, and nothing else.
- **The criterion moved in the direction of survival.**

  | | refute iff | survive iff |
  |---|---|---|
  | first registration (a6533e7) | `rho_hi(6) − rho_hi(3) > 0.40` | `rho_hi(6) − rho_hi(3) <= 0.20` |
  | replacement (b89d5df) | `gap_ratio(6) < 0.50` | `gap_ratio(m) >= 0.50` at every m |

  On this run's data **Δρ_hi = ρ_hi(6) − ρ_hi(3) = +0.3639**, which is neither above 0.40 nor at or below 0.20. **The first registration therefore returns INDETERMINATE on exactly the data the replacement scores as SURVIVES.** Both files define INDETERMINATE as a third outcome for precisely this case.
- **The sentence "nothing was amended after any result existed", carried in the previous version of this finding and in the artifact's notes, is deleted.** It was true of each config file taken individually — neither was edited after a run — and false of the unit, which is the level at which pre-registration means anything. Substituting a criterion by writing a *new* file leaves no diff to catch; that is why it is disclosed here explicitly and why the superseded config, its experiment, and its resolved config are now committed to the record rather than deleted.
- **Headline: K2-T1 is INDETERMINATE.** Reported adjudications, both on the same data:
  - **superseded registration (a6533e7): INDETERMINATE** — Δρ_hi = +0.3639 resolves neither branch. Its other tests hold: collapse(m) = ρ_hi − ρ_lo ≤ −0.40 at every m (−1.203 / −1.078 / −0.947 / −0.704), baseline ρ_lo ≥ 0.55 at every m.
  - **replacement registration (b89d5df): SURVIVES** — on the point estimate gap_ratio(6) = 0.585 ≥ 0.50, plus the ceiling and CI conditions.
  - Where two registrations of one criterion disagree and the earlier one was live when the data were first produced, the earlier binds. The claim's status is **unchanged, not strengthened**.

### The deciding statistic had no interval, and its interval crosses the bar (red-team O-2)

- The replacement config registered "bootstrap CIs on every reported ρ_S" and the principle that **"a point estimate under the ceiling with a CI crossing it does not certify survival"** — but `gap_ratio` is not a ρ_S, so the one statistic the refutation branch is actually decided on reached adjudication as a bare point estimate. Bootstrapped now (**post-hoc**, 4000 resamples, games within cell, both α cells resampled per replicate, m = 3 as the shared denominator):
  **gap_ratio(m = 6) = 0.5853, 95% CI [+0.434, +0.749], P(gap_ratio < 0.50) = 0.129**, margin 1.08 bootstrap SD.
  The interval **crosses the 0.50 bar**. By the registration's own stated principle the surviving branch is not certified, which is the second independent route to INDETERMINATE. This CI is a post-hoc addition made in response to the review, and is labelled as such in the artifact's `effect_sizes` and `notes`; no registered threshold was changed to accommodate it.

### What IS robust, and what the paper's claim now rests on

- **The ceiling criterion, and it is not close.** High-α coupling **never recovers at any tested m**: ρ_hi(m) ≤ 0.35 at every m, and the *upper* interval endpoint is under the ceiling at every m — worst **ci_high = +0.156 against a 0.35 ceiling**. The α = 0.05 and α = 0.95 intervals are **disjoint at every m**. That is what the two-coordinate claim actually needs: independence requires decorrelation, not anti-correlation, and decorrelation is established at every size tested.
- **The ceiling is immune to the substitution, which is the reason it can carry the claim.** `survive_ceiling = 0.35` is *identical* in both registrations (`plane_finite_size.yaml` and `plane_robustness.yaml`), and both impose it in the same CI form (upper endpoint under the ceiling at every m). It is also not a number invented for this unit: it is F-0010's registered collapse threshold, reused so the two units are commensurable. Whichever registration binds, the ceiling reads the same way and passes with room — unlike the primary statistic, which is the only thing the substitution moved.

| m | ρ_S(α=0.05) [95% CI] | ρ_S(α=0.45) | ρ_S(α=0.75) | ρ_S(α=0.85) | ρ_S(α=0.95) [95% CI] | gap | gap ratio |
|---|---|---|---|---|---|---|---|
| 3 | +0.851 [+0.798, +0.889] | +0.869 | +0.627 | +0.398 | **−0.352** [−0.473, −0.226] | 1.203 | 1.000 |
| 4 | +0.806 [+0.741, +0.854] | +0.773 | +0.685 | +0.438 | **−0.272** [−0.405, −0.140] | 1.078 | 0.896 |
| 5 | +0.702 [+0.615, +0.776] | +0.741 | +0.654 | +0.439 | **−0.244** [−0.377, −0.097] | 0.947 | 0.787 |
| 6 | +0.716 [+0.629, +0.788] | +0.698 | +0.607 | +0.388 | **+0.012** [−0.137, **+0.156**] | 0.704 | 0.585 |

- **The replication anchor is EXACT, not approximate.** The seed stream was reconstructed from `dynamics_calibration.py` (PRNGKey(20260808 + 11), fold_in over the *canonical ten-level* index, then fold_in over the game index), so at m = 3 the first 100 games of each cell are **literally the F-0004 games**. Measured ρ_S = +0.32315631563156316 at α = 0.85 and −0.3546834683468347 at α = 0.95 — deviation from the published `chain_comovement.json` values **exactly 0.0** in both cells. This is the strongest single property of the run and it is independent of every criterion dispute above: the m = 3 row is a re-execution of the original reading, not a fresh sample of it.
- **Zero guards fired.** Zero near-critical games, zero non-convergences, zero `make_family` rejections across all 5200 drawn games / 6000 solves. The diagnostic branch of INDETERMINATE (T4(iv)) did not fire; the verdict above is a criterion outcome, not a data-quality one.
- **K2-T2 (onset drift) passes cleanly.** The collapse onset — the smallest swept α at which ρ_S < 0.5 — is **α = 0.85 at every m ∈ {3,4,5,6}**, drift 0.00 against a registered tolerance of 0.10. Where the collapse begins is m-independent.
- **The narrowing of F-0004's headline stands, and it is the useful science.** F-0004 reported that the correlation *"REVERSES SIGN at α = 0.95 (about −0.35)"*. **The sign reversal is an m = 3–5 fact, not a general one**: at m = 6 with fixed Frobenius scale ρ_S = +0.012 with CI [−0.137, +0.156], statistically indistinguishable from zero. **Decorrelation is the m-robust statement**; the sign reversal must be quoted only with its (m, λ_normalised) scope attached.

### The D1 control does not do what the previous version of this finding said it did

- **Withdrawn over-read.** The sentence "at matched effective precision the m-trend vanishes entirely" is **removed**. It is not what the artifact supports. The supported statement is: **the m-trend is largely, not wholly, a λ_normalised effect.**
- **D1 does not hold λ_normalised constant — it drives it the other way.** Measured at α = 0.95: the main arm's λ̄ = λ·(payoff range) falls **1.86 → 1.62 → 1.42 → 1.27 (−32%)** as m goes 3 → 6, while D1's rises **1.85 → 2.18 → 2.38 → 2.58 (+40%)**. The two arms therefore **bracket** the confound rather than controlling it. Interpolating ρ_hi(m = 6) linearly in λ̄ between the two arms to the m = 3 arms' λ̄ ≈ 1.86 gives ρ_hi(6) ≈ −0.13, leaving a **residual m-effect of ≈0.22 out of the observed 0.364** (the red team's estimate on the same construction is ≈0.27; the spread is the interpolation target, not the conclusion). **Most of the m-effect survives matching.** A real control would fix λ̄ by construction, not straddle it.

| m | D1 ρ_S(α=0.05) | D1 ρ_S(α=0.95) [95% CI] | D1 λ̄(α=0.95) | D1 gap | D1 gap ratio |
|---|---|---|---|---|---|
| 3 | +0.852 | −0.347 [−0.516, −0.170] | 1.85 | 1.199 | 1.000 |
| 4 | +0.625 | −0.431 [−0.584, −0.251] | 2.18 | 1.056 | 0.881 |
| 5 | +0.779 | −0.453 [−0.617, −0.258] | 2.38 | 1.232 | 1.027 |
| 6 | +0.747 | −0.300 [−0.471, −0.116] | 2.58 | 1.046 | 0.872 |

- **D1 and D2 are UNPAIRED to the main arm** — different seed offsets (310000 and 510000 against the main arm's F-0004 stream) and 100 games per cell against the main arm's 200 — so every arm-to-arm comparison above carries between-sample noise that a paired design would cancel. **Both should be re-run paired** (same games, same order, only the controlled quantity varying), which is the same discipline K3 already uses and the reason K3's 0.05 bar is meaningful. Registered as the follow-up obligation of this finding.
- **D2 (λ = 1.5, the value originally tasked) has its own gap ratio of 0.529 — a 2.9-point margin against the same 0.50 bar**, on 100 games per cell (null SD 0.100). gap(m=3) = 1.425 (ρ_hi = −0.570), gap(m=6) = 0.754 (ρ_hi = +0.005 [−0.188, +0.215]). Its bootstrap interval is **[+0.368, +0.707] with P(gap_ratio < 0.50) = 0.36** — the bar is barely inside a coin flip. A 2.9-point margin on the *diagnostic* arm is a second signal that the gap-ratio criterion is not resolvable at this n, independently of the CI in O-2. (The tasking's λ = 1.5 was a misquotation of the reference artifact, which is λ = 1.2; that was recorded in the config before the run and is not at issue.)

### K3 is a diagnostic, not a kill-shot passed with margin (framing correction)

- **What was reported**: "K3-T1 passes with the largest margin available — |Δρ_S| = 0.00000 in all four comparisons against a registered 0.05 bar." **What is true**: Spearman is **rank-based**, and the two solver arms perturb ℛ by **~1e-12** (max |Δℛ| = 1.3e-12 mirror, 2.9e-13 tight; max sup-norm |Δσ| = 2.7e-12 and 7.2e-13). A perturbation twelve orders of magnitude below the spacing of adjacent ℛ values in a 100-game sample **cannot reorder them**, so **|Δρ| = 0 was arithmetically forced before the run** rather than discovered by it. The comparison had no power to fail and its "margin" is not evidence.
- **K3 should be reported as the diagnostic it is**: *the solver contributes ~1e-12 to ℛ, and the rank ordering of ℛ is bit-stable under solver replacement.* That is a real and useful statement — it rules out finite-precision path dependence, the stated K3 hypothesis — but it is a measurement of solver noise, not a survived kill-shot. A test with power would have had to perturb ℛ at the scale of the rank spacing (a coarse tolerance, a different fixed point, or a rank-insensitive statistic).
- Note also, unchanged: EPR is computed from the generator and never touches the solver, so the solver can enter this correlation only through ℛ; and two solvers converging to the same fixed point cannot exclude a bias *shared* by both.

### Record corrections to the artifact

- `n` was recorded as 5600. **It is not.** There are **5200 distinct games drawn** (main 4×5×200 = 4000, D1 4×2×100 = 800, D2 2×2×100 = 400) and **6000 solves** (those 5200 once each, plus 2 arms × 2 levels × 100 games × 2 solves in K3, since every K3 game is solved by both the reference and the arm solver). The 400 K3 "games" are **re-draws of games already counted in the main m = 3, α ∈ {0.85, 0.95} cells** — by design, since the paired 0.05 bar requires identical games — so they add solves and comparisons, not sample size. The registered figure added those 400 as if new while omitting the 400 duplicate reference solves; the errors do not cancel. `n = 5200` and the decomposition are now in the artifact's `n_justification`.
- `passed` was `False`-by-construction-of-the-verdict. It now follows the house convention used by `smalln_certification.json` and `decoupling_mechanism.json`: **`passed = True` means the registered adjudication ran and no guard fired**, with the verdict in `metrics`/`notes`. A successful kill-shot or an INDETERMINATE is a legitimate scientific outcome and must not read as a failed run.
- **What this cannot settle, restated from the pre-registration:** N = 2 throughout — nothing here speaks to N-scaling, which is the obvious next kill-shot and is untested. m stops at 6 (36 joint states; the dense Glauber generator is the binding cost, not a correctness limit). Only Gaussian-source Hodge families. K3 tests two solvers, not the space of solvers — and, per the framing correction above, tests them with a statistic that could not have detected a difference of the size they produce.
- Chase status: chasing — (i) re-run D1 and D2 **paired** to the main arm at matched λ̄ by construction, which is the only way to settle how much of the m-effect is dimensional; (ii) the N-scaling kill-shot; (iii) a finite-size criterion that is resolvable at feasible n, since both the gap-ratio CI and the D2 margin say the present one is not.
- Resolution / follow-up: artifacts `benchmarks/results/plane_robustness.json` + `plane_robustness.resolved.yaml`; the superseded registration is preserved at `config/experiments/plane_finite_size.yaml`, `experiments/plane_finite_size.py` and `benchmarks/results/plane_finite_size.resolved.yaml`, all now committed so the "config committed before the experiment" property is checkable rather than vacuous. **The flagship claim is not withdrawn and is not strengthened**: it rests on decorrelation at the ceiling, which is robust at every tested m, and no longer on a finite-size criterion whose two registrations disagree.

## F-0023 — The N-scaling kill-shot does not kill the plane, it kills the *instrument*: low-α co-movement of EPR and ℛ is a TWO-PLAYER fact. The numerator kill-shot passes at every m, and F-0022's "m=6 is indistinguishable from zero" does not survive a real λ̄ control

- Date: 2026-08-13
- Instrument / experiment: `experiments/plane_nplayers.py` (unit `science.plane.nplayers`, R12). Two pre-registered kill-shots on the flagship claim that response asymmetry ℛ (local derivative, χ^eq = (I − SB)⁻¹S) and Schnakenberg EPR (global stationary-flux functional of the Glauber chain) are independent coordinates: **A** the high-α decorrelation is an N = 2 artefact; **B** F-0007's refutation of the ratio-artefact objection is an m = 3 artefact. λ = 1.2, 400 games per cell, percentile bootstrap 2000 resamples on every ρ_S, 40 distinct cells, 16000 games solved from 12000 distinct source draws, 19 minutes of Spark wall-clock.
- Config: `config/experiments/plane_nplayers.yaml`, committed at **b9648f8** BEFORE `experiments/plane_nplayers.py` existed (verified at commit time with `git ls-files`: the config was in the tree, the experiment path was not). **This unit has exactly ONE criteria file and will never have a second** — that is the direct operational lesson of R11's red-team objection O-1 (F-0022), and it is written into the config's own header.

### Verdicts against the registered criteria

| criterion | verdict | why |
|---|---|---|
| **A-T1** (ceiling at N ∈ {2,3,4}, m = 3) | **INDETERMINATE** | the ceiling itself is *comfortably* intact — `holds_raw` is TRUE in **both** arms, worst ci_high **+0.163** against the 0.35 ceiling — but the registered **precondition ρ_lo ≥ 0.55 FAILS at N = 3 and N = 4 in both arms**, and a size with no baseline coupling has no collapse to measure. Registered as INDETERMINATE, reported as INDETERMINATE. |
| **A-T2** (onset drift in N) | **PASS, but VACUOUSLY** | onset(2) = 0.85, onset(3) = onset(4) = 0.05, drift −0.80 against a tolerance of +0.10. The criterion is one-sided, so onset moving *down* passes it — but it moved down because ρ_S is already below 0.5 at the *lowest* α. **A-T2 as registered is uninformative whenever the A-T1 precondition fails**; that is a defect of this registration and it is recorded, not repaired. |
| **B-T1** (ceiling on the NUMERATOR, m ∈ {3,4,5,6}, N = 2) | **HOLDS** | ρ_S(EPR, ‖χ−χᵀ‖) at α = 0.95 is **negative with a negative upper interval endpoint at every m in both arms** (worst ci_high **−0.0067** main, **−0.1076** matched) against a 0.35 ceiling; precondition ρ_num(α=0.05) ≥ 0.55 holds at every m in both arms. |
| **C1** (binding paired λ̄-matched control) | **arms AGREE on both primaries** | `a_arms_agree = 1`, `b_arms_agree = 1`. λ̄ matched to **max \|ratio − 1\| = 0.0314** against the registered 0.10 tolerance. |
| **T4(v)** diagnostics | **no guard fired** | zero near-critical games, zero non-convergences, zero `make_family` rejections across all 16000 solves; `make_family`'s constructed harmonic fraction is exact to ≤ 4.4e-16 at **N = 3 and N = 4**, where its docstring only asserts exactness for two players. |
| **T4(vi)** pairing wiring | **PASS** | max \|Δρ\| between each C1 arm's reference cell and the main-arm cell = **0.00e+00** (resolved through the cell cache: they are literally the same solves). |
| **T4(vii)** replication anchors | **EXACT** | all **seven** anchors reproduce their published values to **exactly 0.000000**: F-0004's ρ_S(EPR, ℛ) at α ∈ {0.85, 0.95} and F-0007's ρ_S(numerator, EPR) at **all five** swept levels, on the first 100 games of every N = 2, m = 3 cell. The m = 3 rows are re-executions of the published readings, not fresh samples. |
| `strong_anticorrelation` flag (reported, not a criterion) | **did not fire** anywhere | most negative ρ_hi observed is −0.345 against a −0.55 trigger. |

`passed = true` follows the house convention: the registered adjudication ran, the anchors hold, no data-quality guard fired. It is not a verdict.

### KILL-SHOT A — the claim does NOT die, but the instrument that established it is two-player-only

**The DIES branch never came close.** It required ρ_hi > 0.35 with ci_low > 0.35 at some N > 2. Measured ρ_hi at α = 0.95, m = 3:

| N | arm | scale | λ̄(α=0.05) | ρ_S(α=0.05) [95% CI] | λ̄(α=0.95) | ρ_S(α=0.95) [95% CI] |
|---|---|---|---|---|---|---|
| 2 | main ≡ C1a | 2.0000 | 1.960 | **+0.8469** [+0.810, +0.878] | 1.855 | **−0.3325** [−0.418, −0.232] |
| 3 | main (fixed) | 2.0000 | 1.202 | +0.2821 [+0.193, +0.373] | 1.217 | +0.0613 [−0.030, +0.152] |
| 3 | **C1a (λ̄-matched)** | 3.1195 | **1.976** | **+0.3776** [+0.290, +0.459] | 1.898 | **+0.0703** [−0.028, +0.163] |
| 4 | main (fixed) | 2.0000 | 0.722 | +0.1466 [+0.051, +0.245] | 0.729 | −0.0143 [−0.119, +0.090] |
| 4 | **C1a (λ̄-matched)** | 5.1258 | **1.958** | **+0.1715** [+0.072, +0.264] | 1.867 | **−0.0089** [−0.105, +0.086] |

High-α coupling is **absent, not recovered**, at N = 3 and N = 4, in both arms. On the ceiling alone the plane looks *more* like a plane at N > 2 than at N = 2.

**But the precondition fails, and this is the finding.** ρ_S(EPR, ℛ) at α = 0.05 falls **+0.847 → +0.378 → +0.172** across N = 2, 3, 4 **at matched effective precision**. The full α profile at fixed scale shows the same thing — at N ≥ 3 the correlation is small at *every* α:

| N | α=0.05 | α=0.45 | α=0.75 | α=0.85 | α=0.95 |
|---|---|---|---|---|---|
| 2 | +0.8469 | +0.8457 | +0.7045 | +0.4111 | −0.3325 |
| 3 | +0.2821 | +0.1232 | +0.0815 | +0.0571 | +0.0613 |
| 4 | +0.1466 | −0.0066 | +0.0398 | +0.1259 | −0.0143 |

**The α-stratified collapse — the programme's central demonstration since F-0003/F-0004 — does not exist at N ≥ 3, because there is nothing to collapse from.** gap(N) = ρ_lo − ρ_hi with intervals: **+1.179** [+1.085, +1.273] at N = 2, **+0.221** [+0.093, +0.353] at N = 3, **+0.161** [+0.015, +0.300] at N = 4 (matched arm: +1.179 / +0.307 / +0.180). Reported as data — the gap-shrink family of criteria was **deliberately not registered** here, because F-0022 established it is not resolvable at feasible n.

**It is not a temperature artefact, and it is not meter degeneracy.** Both were checked, both were pre-registered as things the control had to exclude:
- **Temperature.** C1a fixes median λ̄ by construction from a disjoint calibration draw: λ̄ at α = 0.05 is 1.960 (N=2), 1.976 (N=3), 1.958 (N=4). Matching *raises* ρ_lo(3) only from +0.282 to +0.378 and ρ_lo(4) from +0.147 to +0.172 — nowhere near the 0.55 floor, let alone N = 2's +0.847.
- **Degeneracy.** At matched λ̄ the meters have comparable magnitudes across N, so the decorrelation is not two near-constant columns: at α = 0.05, median EPR = 1.20e-3 / 1.26e-3 / 1.21e-3 and median ℛ = 0.0187 / 0.0103 / 0.0054 for N = 2 / 3 / 4; at α = 0.95, median EPR = 0.547 / 0.442 / 0.403 and median ℛ = 0.342 / 0.173 / 0.096. Median distance-to-criticality is 0.48–0.94 everywhere; nothing is near the wedge.

**Reading.** ℛ is a local derivative on a Σ(mᵢ−1)-dimensional tangent space; EPR is a global flux functional on a ∏mᵢ-state chain. At N = 2 there is exactly one non-trivial multi-player Hodge sector, so a single harmonic modulator drives both meters and they co-move at ρ ≈ +0.85. At N = 3 there are four such sectors and at N = 4 eleven, and the two functionals weight them differently. **Hypothesis with a stated test** (not a claim): regress per-game ℛ and EPR on the per-sector harmonic norms at N = 3 and check whether the sector loadings differ; that is one pass over data this run already has the machinery to produce.

### KILL-SHOT B — F-0007 generalises in m, and the numerator and the ratio turn out to be the *same statistic*

ρ_S(EPR, ‖χ−χᵀ‖) at α = 0.95, N = 2, with the ratio on the **identical games** and their **paired** difference:

| m | λ̄ | NUMERATOR ρ_S [95% CI] | RATIO ρ_S [95% CI] | δ = num − ratio [paired 95% CI] |
|---|---|---|---|---|
| 3 | 1.855 | **−0.3447** [−0.435, −0.254] | −0.3325 [−0.424, −0.235] | −0.0122 [−0.0158, −0.0091] |
| 4 | 1.623 | **−0.2574** [−0.348, −0.163] | −0.2536 [−0.340, −0.161] | −0.0038 [−0.0056, −0.0023] |
| 5 | 1.414 | **−0.1780** [−0.274, −0.080] | −0.1760 [−0.268, −0.081] | −0.0019 [−0.0035, −0.0007] |
| 6 | 1.268 | **−0.1128** [−0.213, −0.007] | −0.1119 [−0.214, −0.016] | −0.0009 [−0.0019, +0.0001] |

Baseline ρ_num(α = 0.05) = +0.773 / +0.751 / +0.667 / +0.683, all above the 0.55 floor. **B-T1 HOLDS**: the numerator's decoupling is not an m = 3 fact, and the ratio-artefact objection stays refuted at every tested m.

**The sharper form of the refutation, which the paired design bought and F-0007 could not state.** δ(m) is not merely small, it is **bounded by 0.012 and shrinking with m**: the numerator and the ratio produce the *same rank ordering against EPR* to within about 1%, on identical games. The objection was that ℛ's collapse is its denominator misbehaving; the measurement is that removing the denominator changes the answer by less than one part in eighty.

**Why**, and it generalises F-0007's H2 in both directions: ρ_S(ℛ, 1/‖χ+χᵀ‖) at α = 0.95 is **+0.9954 / +0.9942 / +0.9921 / +0.9919** across m = 3…6 and **+0.9954 / +0.9999 / +0.9999** across N = 2, 3, 4 — and at α = 0.05 it is ≈ 0 everywhere (−0.24 … +0.05). **F-0007's H2 (high-α denominator dominance of ℛ) is robust in m AND in N**, which is a free generalisation of a result that was also previously single-cell.

### A correction to F-0022: the m = 6 "indistinguishable from zero" was largely a λ̄ artefact

F-0022's narrowing said: *"The sign reversal is an m = 3–5 fact, not a general one: at m = 6 with fixed Frobenius scale ρ_S = +0.012 with CI [−0.137, +0.156]."* Under the **paired, λ̄-matched** control registered here (C1b), at n = 400:

| m | scale | λ̄ | C1b ρ_S(EPR, ℛ) at α=0.95 [95% CI] | main-arm (fixed scale) ρ_S [95% CI] |
|---|---|---|---|---|
| 3 | 2.0000 | 1.855 | −0.3325 [−0.423, −0.246] | −0.3325 [−0.424, −0.235] |
| 4 | 2.3535 | 1.909 | **−0.2966** [−0.387, −0.204] | −0.2536 [−0.340, −0.161] |
| 5 | 2.6308 | 1.860 | **−0.2554** [−0.345, −0.160] | −0.1760 [−0.268, −0.081] |
| 6 | 3.0171 | 1.913 | **−0.2061** [−0.300, −0.105] | −0.1119 [−0.214, −0.016] |

At matched effective precision **the m = 6 interval excludes zero** and the sign reversal survives to m = 6. Two things moved the number: doubling n (R11's +0.012 at n = 200 and this run's −0.113 at n = 400 are the same games plus 200 more, and each lies inside the other's interval — no contradiction, just resolution), and controlling λ̄ (the fixed-scale arm drives λ̄ from 1.855 down to 1.268 across m; C1b holds it at 1.86–1.91). Of the fixed-scale drift in ρ_hi from m = 3 to m = 6, **+0.221 total, +0.126 survives matching and +0.095 is temperature**. F-0022's estimate of the residual was an interpolation between two unpaired arms; this is a paired, matched measurement, and it says F-0022's narrowing was too strong on this point. **F-0022's follow-up obligation (i) — "re-run D1 and D2 paired to the main arm at matched λ̄ by construction" — is DISCHARGED**, on the m axis by C1b and on the N axis by C1a; its follow-up (ii), the N-scaling kill-shot, is **EXECUTED** and is this finding.

### What this does to the flagship claim

**It does not die, it does not straightforwardly strengthen, and the honest summary is different on each axis.**

- **On the m axis it is STRONGER.** Kill-shot B passed in both arms with the upper interval endpoint *negative* at every m; the ratio-artefact objection is refuted with a paired bound of 0.012 rather than a single point estimate; and the α = 0.95 anti-correlation is now m-robust to m = 6 at matched λ̄ instead of vanishing at m = 6.
- **On the N axis it is NARROWED, and narrowed at the level of the evidence rather than the claim.** The ceiling holds at N = 3 and N = 4 with the widest margin the programme has recorded (worst ci_high +0.163), which is *consistent* with two independent coordinates. But the α-stratified collapse — the design that made the claim persuasive, and the source of every number in F-0003, F-0004, F-0007, F-0010 and F-0022 — **is a two-player instrument**. At N ≥ 3 the meters do not couple at low α, so the collapse cannot be demonstrated, and this run declines to certify the claim at N > 2 rather than reading absence-of-coupling-everywhere as confirmation.
- **The claim's status is therefore UNCHANGED at N > 2 and its supporting narrative is narrowed to N = 2.** The papers must not say "EPR and ℛ co-vary while a potential component modulates both, and part company when it vanishes" without attaching **N = 2** to it, in the same way F-0022 required (m, λ̄) attached to the sign reversal.

### Registered limitations, restated

N stops at 4 and m stops at 6, and the two axes were swept **separately** (A at m = 3, B at N = 2) — the (N, m) interior is unread. One λ in both arms; F-0010 already showed the α = 0.95 sign is λ-dependent, so no sign statement here generalises in λ. Only Gaussian-source Hodge families. C1 matches the **median** λ̄ per cell, not the distribution, and controls for nothing else that changes with N or m (tangent dimension, ∏mᵢ joint states, the number of Hodge sectors). The ceiling criterion certifies **decorrelation**, which is necessary and not sufficient for independence — a non-monotone dependence would pass it, and that limitation belongs to the whole programme, not to this run. N = 4 (81 joint states) is inside the dense-dynamics 400-state guard but **outside the API's ≤ 3-player surface guard**, so the N = 4 row is a library-level reading that cannot be served.

- Chase status: **chasing** — (a) the sector-loading regression named above, which is the mechanism test for why low-α co-movement is two-player-only; (b) a criterion for the N axis that does not depend on a low-α baseline, since this run shows that baseline does not exist at N ≥ 3 and A-T1's precondition therefore makes the N sweep unadjudicable by construction; (c) A-T2's one-sidedness, which lets onset drift *downward* into a vacuous pass. Items (b) and (c) are defects of **this** registration, recorded here and deliberately **not** repaired by writing a second criteria file for this unit.
- Resolution / follow-up: artifacts `benchmarks/results/plane_nplayers.json` + `plane_nplayers.resolved.yaml`. p1_instruments gains "N = 2" on the instrument-scope table's α-regime row; p3_noneq's decoupling section gains the N-scaling result and the F-0022 correction; `memory/claims.md` C1 gains the N-scope qualifier.
