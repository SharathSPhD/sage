Every quantitative statement in this paper maps to a machine-readable artifact
in \texttt{benchmarks/\allowbreak results/} (JSON, schema
\texttt{strataq\_bench.BenchmarkResult}: metrics, effect sizes with intervals,
$n$, $n$-justification, seed, configuration reference, library version,
timestamp). All regenerate with \texttt{make reproduce} (seed 20260808 unless
stated). The gate records in \texttt{gates/*.yaml} carry, for each unit, the
claim, the section-by-section pass state, and the adversarial reviewer's
objections with their dispositions.

\begin{center}\footnotesize
\setlength{\tabcolsep}{4pt}
\begin{tabular}{@{}>{\raggedright\arraybackslash}p{6.9cm}>{\raggedright\arraybackslash}p{8.3cm}@{}}
\toprule
Statement in text & Artifact \\
\midrule
Calibration table (Table~\ref{tab:calib}) & the artifacts named in its rows \\
$\chieq$ vs finite differences, $1.3\times10^{-8}$ & \texttt{chi\_fd\_agreement.json} \\
Implicit-diff VJP vs the resolvent, $5.6\times10^{-17}$ & \texttt{implicit\_chi\_agreement.json} \\
\texttt{pygambit} fixed-$\lambda$ agreement, $1.2\times10^{-9}$ & \texttt{gambit\_agreement.json} \\
Branch tracer agreement, $8.6\times10^{-13}$ & \texttt{branch\_agreement.json} \\
Mirror descent vs damped solver, $6.9\times10^{-13}$ & \texttt{solver\_cross\_agreement.json} \\
Sioux Falls: $\R$, $DF$ defect, Fisk KKT, UE diagnostic gap & \texttt{sioux\_falls\_calibration.json} \\
Engine-2 identities (Beckmann gradient, KKT, $DF$, toll-$\chi$) & \texttt{population\_identities.json} \\
Blotto readings incl.\ the degenerate null & \texttt{blotto\_readings.json} \\
$\R$ on potential / harmonic games & \texttt{reciprocity\_potential.json}, \texttt{reciprocity\_harmonic.json} \\
Gibbs stationarity; $\EPR$ and $J^*$ zeros; $SB$ spectrum & \texttt{gibbs\_agreement.json}, \texttt{equilibrium\_reads\_zero.json}, \texttt{spectrum\_reality.json} \\
KLD rank correlation $1.0$; TUR median tightness $0.850$ & \texttt{estimator\_alpha\_sweep.json}, \texttt{estimator\_ground\_truth.json} \\
Toolkit facade reproduces its research numbers & \texttt{toolkit\_verdicts.json} \\
AQRE vs \texttt{pygambit} extensive-form logit & test suite (\texttt{tests/extensive/}), \emph{not} a committed artifact --- see Section~8 \\
\bottomrule
\end{tabular}
\end{center}

Two pointers, and a correction to an earlier draft of this appendix. The
findings log (\texttt{memory/findings.md}) records each anomaly with the
configuration that produced it, its chase status and its resolution, including
retractions --- an earlier version of this appendix stated that the log covered
entries \textbf{F-0001--F-0007}, which was true of a draft written in August
2026 and is now stale by seventeen entries. The log currently runs
\textbf{F-0001--F-0024}, and the entries a reader of \emph{this} paper should
look at are F-0002 (the ``$\R$ is $\lambda$-free'' claim corrected to apply only
to the zero test), F-0005 (Blotto is mixed, not pure harmonic), F-0011 (the
first empirical $\R$), and F-0018 (the release-integrity failure of
Section~7). The claim ledger (\texttt{memory/claims.md}) carries every
scientific claim's confidence tier and its history, including downgrades; the
architecture decisions (\texttt{memory/decisions.md}) carry every divergence
from the plan, including the two units currently on the main branch whose gates
are red.
