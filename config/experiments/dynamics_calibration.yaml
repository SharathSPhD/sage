# Dynamics/thermo calibration — the dynamics.exact domain-gate artifacts.
defaults:
  - _self_

seed: 20260808
lam: 1.2
lam_grid: [0.7, 1.2, 2.0]

sweep:
  levels: [0.05, 0.15, 0.25, 0.35, 0.45, 0.55, 0.65, 0.75, 0.85, 0.95]
  games_per_level: 100     # generator assembly is the cost driver; precision
                           # reasoning in the artifact
  shape: [3, 3]
  scale: 2.0

bootstrap:
  n_resamples: 2000
